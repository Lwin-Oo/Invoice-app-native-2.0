const express = require ('express');
const bodyParser = require ('body-parser');
const cors = require ('cors');
const mongoose = require ('mongoose');
const AuthService = require('./BackendAuthService');

const app = express();

//Middleware

//Connect to MongoDB

//define routes

//Start the server